<tbody>
	<% if name.kind_of?(Athlete) %>
		<td><%= link_to name.logname, [@sport, name] %></td>
	<% elsif name.kind_of?(Gameschedule) %>
		<td><%= link_to ("vs " + name.opponent), [@sport, @sport.teams.find(name.team_id), name] %></td>
	<% else %>
		<td><%= name %></td>
	<% end %>
	<td><%= stat.tackles %></td>
	<td><%= stat.assists %></td>
	<td><%= stat.sacks %></td>
	<td><%= stat.interceptions %></td>
	<td><%= stat.pass_defended %></td>
	<td><%= stat.int_yards %></td>
	<td><%= stat.int_long %></td>
	<td><%= stat.int_td %></td>
	<td><%= stat.fumbles_recovered %></td>
	<td><%= stat.safety %></td>
	<% if FootballDefense.find(stat.id).nil? and isAdmin? or isTeamManager?(player) and name != "Totals" %>
		<td><%= link_to "Edit", new_sport_athlete_football_defense_path(@sport, player, gameschedule_id: stat.gameschedule_id), 
				class: "btn btn-xs btn-primary"%></td>
	<% elsif name != "Totals" %>
		<td><%= link_to "Edit", edit_sport_athlete_football_defense_path(@sport, player, stat), class: "btn btn-xs btn-primary"%></td>
	<% else %>
		<td></td>
	<% end %>
</tbody>
