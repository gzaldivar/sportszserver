<tbody>
	<% if name == "Player" %>
		<% @theplayer = Athlete.find(stat.athlete_id) %>
		<td><%= link_to image_tag(getimage(@theplayer, 'tiny'), class: 'img-responsive'), [@sport, @theplayer] %>
			<%= link_to @theplayer.numlogname, [@sport, @theplayer] %></td>
	<% elsif name == "Game" %>
		<% @game = Gameschedule.find(stat.gameschedule_id) %>
		<td><%= link_to ("vs " + @game.opponent), [@sport, @sport.teams.find(@game.team_id), @game] %></td>
	<% else %>
		<td><%= name %></td>
	<% end %>
	<td><%= stat.attempts %></td>
	<td><%= stat.completions %></td>
	<td><%= number_with_precision(stat.comp_percentage, precision: 2) %></td>
	<td><%= number_with_precision(stat.compaverage, precision: 2) %></td>
	<td><%= stat.yards %></td>
	<td><%= stat.td %></td>
	<td><%= stat.firstdowns %></td>
	<td><%= stat.interceptions %></td>
	<td><%= stat.sacks %></td>
	<td><%= stat.yards_lost %></td>
	<td><%= stat.twopointconv %></td>
	<% if site_owner? or isTeamManager?(name == "Player" ? @theplayer : @game) and name == "Player" %>
		<td>
			<% if FootballPassing.find(stat.id).nil? %>
				<%= link_to "Update", sport_athlete_football_passing_path(@sport, @theplayer, gameschedule_id: @gameschedule.id), 
										class: "btn btn-xs btn-info"%>
			<% else %>
				<%= link_to "Update", sport_athlete_football_passing_path(@sport, @theplayer, stat_id: stat.id), class: "btn btn-xs btn-info"%>
			<% end %>
		</td>
	<% else %>
		<td></td>
	<% end %>
</tbody>
