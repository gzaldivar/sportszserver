<%= simple_form_for [@sport, @athlete, @stat, @specialteams] do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<%= f.input :koattempts, label: "Punt Attempts" %>
	<p>
	<%= f.input :kotouchbacks, label: "Total Punt Yards" %>
	<p>
	<%= f.input :koreturned, label: "Longest" %>
	<p>
	<%= f.button :submit, 'Submit', class:	"btn btn-medium btn-primary" %>
<% end %>
