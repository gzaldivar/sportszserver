<header class="navbar navbar-default navbar-fixed-top">
  	<div class="navbar-header">
       <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if current_site? %>
      	<a class="navbar-brand" href=<%= "/sports/" + current_site.id %>><%= current_site.sitename %></a>      	
      <% else %>
		<a class="navbar-brand" href="/">Eazesportz.com</a>      
      <% end %>
  	</div>
  	<div class="navbar-header navbar-left">
		<nav class="collapse navbar-collapse" role="navigation">
      		<ul class="nav navbar-nav">
				<li><%= link_to "Contact", contacts_path %></li>
	        </ul>
	        <ul class="nav navbar-right navbar-nav">
				<% if signed_in? and site_owner? %>
					<% if current_team? %>
						<li id="fat-menu" class="dropdown">
							<a href="#" class="dropdown-toggle" data-toggle="dropdown">More ...<b class="caret"></b></a>
				    			<ul class="dropdown-menu">
				        			<li><%= link_to "Coaches", sport_coaches_path(current_site, team_id: current_team.id) %></li>
									<li class="divider"></li>
									<li><%= link_to "Events", sport_events_path(current_site, team_id: current_team.id) %></li>
				    			</ul>
							</a>
						</li>
					<% end %>
					<li id="fat-menu" class="dropdown">
						<a href="#" class="dropdown-toggle" data-toggle="dropdown">Tools<b class="caret"></b></a>
			    			<ul class="dropdown-menu">
			        			<li><%= link_to "Manage Players", sport_athletes_path(current_site) %></li>
								<li class="divider"></li>
								<li><%= link_to "Manage Coaches", sport_coaches_path(current_site) %></li>
								<li class="divider"></li>
								<li><%= link_to "Manage Users", users_path(site: current_site) %></li>
								<li class="divider"></li>
								<li><%= link_to "Manage Sponsors", sport_sponsors_path(current_site) %></li>
			    			</ul>
						</a>
					</li>
					<li id="fat-menu" class="dropdown">
						<a href="#" class="dropdown-toggle" data-toggle="dropdown">
				            <% if user_signed_in? %>
				                <%= current_user.email %> <b class="caret"></b>
		              		<% end %>
						</a>
						<ul class="dropdown-menu">
		                	<% if user_signed_in? %>
								<li><%= link_to "My Profile", user_path(current_user) %></li>
								<% if isAdmin? and !@sport.nil?  %>
								<% end %>
								<li class="divider"></li>
								<li><%= link_to "Change Site", sports_path %></li>
								<li class="divider"></li>
								<li><%= link_to "Sign out", destroy_user_session_path, method: "delete" %></li>
	                		<% end %>
						</ul>
					</li>
				<% elsif !signed_in? %>
					<li>
						<!--%= form_for(resource, :as => resource_name, :url => user_session_path, html: { class: 'form-inline' } ) do |f| %-->
						  		<!--%= f.text_field :email, placeholder: 'Enter Email' %-->
						  		<!--%= f.password_field :password, placeholder: 'Password' %-->
						  		<!--%= f.check_box :remember_me %-->
						  		<!--%= f.label :remember_me %-->
						  		<!--%= f.submit 'Sign in', class: "btn btn-primary btn-xs" %-->
						  		<!--%= link_to "Register", new_registration_path(resource_name) %--><!--br/>
						  	</div-->
						<!--% end %-->	
					</li>				
					<li><%= link_to "Sign in", new_user_session_path %></li>
				<% end %>
			</ul>
		</div>
	</h5>
	</div>
  </div>
</header>

