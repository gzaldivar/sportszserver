<header class="navbar navbar-default navbar-fixed-top">
  	<div class="navbar-header">
       <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".nav-collapse">
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <% if current_site? %>
      	<a class="navbar-brand" href=<%= "/sports/" + current_site.id %>><%= current_site.sitename %></a>      	
      <% else %>
		<a class="navbar-brand" href="/">Eazesportz.com</a>      
      <% end %>
  	</div>
  	<div class="navbar-header navbar-left">
		<nav class="collapse navbar-collapse" role="navigation">
      		<ul class="nav navbar-nav">
	  			<% if @sport.nil? %>
					<li><%= link_to "Contact", contacts_path %></li>
		        <% elsif !@sport.teams.nil? and @sport.teams.count == 0 %>
					<li><%= link_to "Contact", contacts_path %></li>
		        <% else %>
					<li><%= link_to "Contact", contacts_path %></li>
          		<% end %>
	        </ul>
	        <ul class="nav navbar-right navbar-nav">
				<% if signed_in? %>
					<li id="fat-menu" class="dropdown">
						<a href="#" class="dropdown-toggle" data-toggle="dropdown">
				            <% if user_signed_in? %>
				                <%= current_user.email %> <b class="caret"></b>
		              		<% end %>
						</a>
						<ul class="dropdown-menu">
		                	<% if user_signed_in? %>
								<li><%= link_to "My Profile", user_path(current_user) %></li>
								<% if isAdmin? and !@sport.nil?  %>
							  		<!--li class="dropdown-submenu">
							    		<a tabindex="-1" href="#">Admin</a>
							    			<ul class="dropdown-menu">
							        			<li><%= link_to "Manage Players", sport_athletes_path(@sport) %></li>
												<li class="divider"></li>
												<li><%= link_to "Manage Coaches", sport_coaches_path(@sport) %></li>
												<li class="divider"></li>
												<li><%= link_to "Manage Users", users_path(site: current_site) %></li>
												<li class="divider"></li>
												<li><%= link_to "Manage Sponsors", sport_sponsors_path(@sport) %></li>
							    			</ul>
										</a>
									</li-->
								<% end %>
								<li class="divider"></li>
								<li><%= link_to "Change Site", sports_path %></li>
								<li class="divider"></li>
								<li><%= link_to "Sign out", destroy_user_session_path, method: "delete" %></li>
	                		<% end %>
						</ul>
					</li>
				<% elsif !signed_in? %>
					<li><%= link_to "Sign in", new_user_session_path %></li>
				<% end %>
			</ul>
		</div>
	</h5>
	</div>
  </div>
</header>

