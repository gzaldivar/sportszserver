<script>
  $(document).ready(function() {
    $('.coachselected').click(function() {
      $.ajax({
        url: "<%= sport_coaches_path(current_site, team_id: current_team.id) %>",
        dataType: "script"
      });
    });
   $('.eventselected').click(function() {
      $.ajax({
        url: "<%= selectteam_sport_path(current_site, team_id: current_team.id) %>",
        dataType: "script"
      });
    });
  });
</script>
<div class-"row">
  <div class="col-xs-4">
    <div id="moreselect-responsive" class="modal fade" tabindex="-1" data-width="760" style="display: none;">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
        <h4 class="modal-title">More ....</h4>
      </div>
      <div class="modal-body">
        <div class="row">
          <div class="well">
            <div class="col-xs-6">
              <table class="table table-striped table-bordered table-hover">
                <thead>
                  <th>More .....</th>
                </thead>
                <tbody>
                    <tr>
                      <td><%= link_to "Coaches", '#', :class => 'coachselected' %></td>
                    </tr>
                     <tr>
                      <td><%= link_to "Events", '#', :class => 'eventselected' %></td>
                    </tr>
               </tbody>
              </table>
            </div>
          </div>
        </div>
      </div>
      <div class="modal-footer">
        <div class="col-xs-1">
          <button type="button" data-dismiss="modal" class="btn btn-default">Close</button>
      </div>
      </div>
    </div>
  </div>
</div>
