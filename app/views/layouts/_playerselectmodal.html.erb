<div class-"row">
  <div class="col-xs-6">
    <div id="<%= modalname %>" class="modal fade" tabindex="-1" data-width="760" style="display: none;">
      <div class="modal-header">
        <button type="button" class="close" data-dismiss="modal" aria-hidden="true">×</button>
        <h4 class="modal-title">Select Player</h4>
      </div>
      <div class="modal-body">
        <div class="row">
          <div class="col-md-6">
            <table class="table table-striped table-bordered table-hover">
              <thead>
                <th>Player</th>
              </thead>
              <tbody>
                  <% @sport.athletes.where(team_id: current_team.id).asc(:number).each do |p| %>
                    <tr>
                      <td><%= link_to p.numlogname, '#', class: callbackscript, data: {:id => p.id.to_s} %></td>
                    </tr>
                  <% end %>
              </tbody>
            </table>
          </div>
        </div>
      </div>
      <div class="modal-footer">
        <div class="col-xs-1">
          <button type="button" data-dismiss="modal" class="btn btn-default">Close</button>
        </div>
      </div>
    </div>
  </div>
</div>
