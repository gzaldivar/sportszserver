<% provide(:title, current_site.sitename + " - News Item") %>
<p>
	<% item=[]
	  if !@newsfeed.kind_of?(Array) 
	    item[0] = @newsfeed  
	  else
	    item = @newsfeed 
	  end %>
<div class ="row">
	<div class="col-xs-11 col-xs-offset-1">
  		<%= render 'newsfeed', feed: item %>
	</div>
</div>
