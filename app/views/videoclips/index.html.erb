<% provide(:title, current_site.sitename + " Videoclips" ) %>
<script>
  $(document).ready(function() {
    $('#select_game').change(function() {
      $.ajax({
        url: "<%= updategamelogs_sport_videoclips_path %>",
        data: {
          gameid : $('#select_game').val()
        },
        dataType: "script"
      });
    });
  });
</script>
<p> 
<p>
<div class="row">
    <div class="span9 offset1">
        <%= form_tag sport_videoclips_path, class: "form-horizontal", method: 'get' do %>
            <%= hidden_field_tag :team_id, @team.id %>
            <div class="row">
                <div class="span2">
                    <%= label_tag "Game: " %>
                    <%= collection_select(:game, :id, @gameschedules, :id, :game_name, { include_blank: true }, { id: "select_game" } ) %>
                </div>
                <div class="span4 offset1">
                    <%= label_tag "Play: " %>
                    <%= collection_select(:gamelog, :id, @gamelogs, :id, :logentrytext, { include_blank: true }, { id: "gamelogs_select" } ) %>
                </div>
            </div>
            <div class="row">
                <div class="span2">
                    <%= label_tag "Player Number: " %>
                    <%= select(:number, :id, @sport.athletes.collect{ |t| [t.number, t.id] }, include_blank: true ) %>
                </div>
                <div class="span2 offset1">
                    <%= label_tag "Player Name: " %>
                    <%= select(:athlete, :id, @sport.athletes.collect{ |t| [t.name, t.id] }, include_blank: true ) %>
                </div>
            </div>
            <div class="row">
                <% if user_signed_in? %>
                    <div class="span2 offset1">
                        <%= label_tag "My Video Clips: " %>
                        <%= check_box_tag "username" %>
                    </div>
                <% end %>
            </div>
            <br/>
            <div class="row">
                <div class="span1 offset3">
                  <%= submit_tag  "Search", class: "btn btn-medium btn-primary", name: nil %>
                </div>
            </div>
        <% end %>
        <%= render @videoclips %>
    </div>
    <div class="row">
        <div class="span3 offset4">
            <% if !@team.nil? and user_signed_in? and user_pics_ok? %>
                <%= link_to "Add Team Video Clip", newteam_sport_videoclip_path(@sport, @team), class: "btn btn-medium btn-primary" %>
            <% end %>
        </div>
    </div>
</div>
